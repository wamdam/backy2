Backlog:

* Write tests for 100% coverage
* Add more statistics
* Add tests for anything where scrub marks blocks as invalid (source changed,
  bitrod in backup, ...

Next:

* Add a backy-wide lock to prevent other backys running during full cleanup
